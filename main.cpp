#include <iostream>
//#include <jsoncpp/json/value.hpp> //ignorar error: es porque no está instalada en windows pero si en el entorno de Ubuntu, si compila
//#include <jsoncpp/json/json.hpp> //ignorar error: es porque no está instalada en windows pero si en el entorno de Ubuntu, si compila
#include <fstream>

int main(){
    return 0;
}